# Chave da Google Maps SDK for Android (usada em app.config.js).
# Copie este arquivo para `.env` e preencha com a sua chave.
# Sem ela o mapa nativo do Android aparece em branco; iOS e web nao precisam.
GOOGLE_MAPS_API_KEY=
